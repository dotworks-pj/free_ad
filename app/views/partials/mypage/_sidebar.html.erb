<div class="sidebar col-md-4">
  <ul class="menu-list">
    <li class="<%= 'is-active' if controller.controller_name == 'user_profiles' %>">
      <% if current_user.profile&.persisted? %>
        <%= link_to '自社プロフィール', edit_mypage_user_profile_path(current_user.profile) %>
      <% else %>
        <%= link_to '自社プロフィール', new_mypage_user_profile_path %>
      <% end %>
    </li>
    <li class="<%= 'is-active' if is_active?(mypage_places_path) %>">
      <%= link_to 'マイ場所一覧', mypage_places_path %>
    </li>
    <li class="<%= 'is-active' if is_active?('#') %>">
      <%= link_to '売上確認', '#' %>
    </li>
    <li class="<%= 'is-active' if is_active?('#') %>">
      <%= link_to '会員情報変更', '#' %>
    </li>
    <li class="<%= 'is-active' if is_active?('#') %>">
      <%= link_to 'メールアドレス変更', '#' %>
    </li>
    <li class="<%= 'is-active' if is_active?('#') %>">
      <%= link_to 'パスワード変更', '#' %>
    </li>
  </ul>
</div>
